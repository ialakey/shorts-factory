[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    render: реально рендерит видео через moviepy/ffmpeg — медленно, но проверяет этапы «как в бою»
    ffmpeg: требует ffmpeg/ffprobe в PATH
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
