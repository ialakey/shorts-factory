# --- Пайплайн ---
moviepy==1.0.3
numpy==2.1.3
tqdm==4.66.6
requests==2.32.3
openai==0.28.0
git+https://github.com/openai/whisper.git
imageio[ffmpeg]
decorator
Pillow==11.0.0
PyYAML==6.0.2
python-dotenv==1.1.1

# --- Компьютерное зрение (детектор лиц для виртуальной камеры) ---
opencv-python==4.10.0.84
mediapipe==0.10.14   # необязательный бэкенд: без него работает fallback YuNet → Haar

# --- Скачивание исходников (этап kodik_download) ---
anime_parsers_ru==1.17.0
lxml==5.2.1

# --- Рендер эмодзи в субтитрах ---
pilmoji==2.0.4
# pilmoji 2.0.4 требует emoji 1.x: get_emoji_unicode_dict удалён в emoji 2.0.0
emoji==1.7.0
